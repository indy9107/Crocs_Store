# The only thing the browser needs: the URL of our Cloudflare Worker backend.
# Not a secret — the Worker holds all D1/R2 credentials via bindings.
# Fill in the real URL after `npx wrangler deploy` (see worker/README or plan).
VITE_API_BASE_URL=https://crocs-store-api.croc-store.workers.dev